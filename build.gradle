apply from: '../RLibrary/lib.gradle'

// 如果出错, 请将以下代码, 同时复制到APP的gradle文件中
android {
    defaultConfig {
        manifestPlaceholders = [
                //bmob 后台申请的app id
                BMOB_APP_ID : "a5809b8e9d877c2e88fd655eac533bed",
                //渠道统计
                BMOB_CHANNEL: "bmob_student_manager"
        ]
    }

    //记得调用 RBmob.init() 方法哦

    useLibrary 'org.apache.http.legacy'
}
dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api project(':RLibrary:uiview')
}
